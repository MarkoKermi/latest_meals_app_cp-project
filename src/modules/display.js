const modalWindow = document.querySelector('.popup-container');
const comment = document.querySelectorAll('.comment');
const Cards = document.querySelector('.cards');
const ProductsCounter = document.querySelector('.products-count');
export {
  modalWindow, ProductsCounter, comment, Cards,
};
